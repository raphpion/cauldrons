version: '3.1'
services:
  postgres:
    image: postgres:latest
    ports:
      - 5432:5432
    env_file:
      - .postgres.env
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
